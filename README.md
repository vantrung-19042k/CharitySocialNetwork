# CharitySocialNetwork
This is a project running on the _Python_ platform:
  - Use _Django, Django Rest Framework_ (Backend) to build APIs for an application running on web platform.
  - Use _MySQL_ to build database.
